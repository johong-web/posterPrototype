# Image tracking template

Minimal Vite starter for **Image tracking** with `@web-ar-studio/webar-engine-sdk`.

## Run

```bash
npm install
npm run dev
```

If needed, copy `.env.example` to `.env` and set `VITE_API_KEY`.
